{
  "name": "@pi-workbench/tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist/src"
  ],
  "dependencies": {
    "yaml": "2.9.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/tests/*.test.js"
  }
}
